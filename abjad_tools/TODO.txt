MusicMaker:
    1.) Fix rhythm staff when pitches are not declared.

AttachmentHandler:

    3.) Fix Clef declaration (MAYBE a list of clefs associated with register based on shelf)
    4.) Add optional ottava mark with shelf keyword

    6.) Add notehead alterations that aren't currently present and rename
    7.) Fix Text spanners
    8.) Add woodwind diagram interface?
    9.) Add slur/tie interface?
    10.) Add optional keyword to attach dynamic to either each leaf in run OR boundaries of run

    11.) encapsulate glissando and rename

    12.) piano pedal?

    13.) add trill handler that replaces chords with trills

other:
    1.) Package tools to be imported
    2.) Encapsulate random walks
    3.) Random walk rmaker
    4.) Markov rmaker
    5.) Markov pitch tool
    6.) work with 12 tone row object
    7.) Encapsulate timespan dictionary creation
    8.) Simplify score structure (base it only on timespans lists in file)
    9.) Write tool to split timespans by measure boundaries!
